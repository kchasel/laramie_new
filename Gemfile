source 'http://rubygems.org'

gem 'rails', '3.1.0'

# Bundle edge Rails instead:
# gem 'rails', :git => 'git://github.com/rails/rails.git'

gem 'mysql2', '>=0.3'

# gem 'mongrel', '>= 1.2.0.pre2'

gem 'thin', '1.2.11'

group :assets do
  gem 'sass-rails', "  ~> 3.1.0"
  gem 'coffee-rails', "~> 3.1.0"
  gem 'uglifier'
end

# Reactivate error_message form helpers for views
gem 'dynamic_form'
gem 'rails_autolink'

# Listhost-related gems
gem 'delayed_job'
gem 'ar_mailer_rails3'

# Use unicorn as the web server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# Bundle the extra gems:
# gem 'bj'
# gem 'nokogiri'
# gem 'sqlite3-ruby', :require => 'sqlite3'
# gem 'aws-s3', :require => 'aws/s3'

# icalendar to support generation of .ics files
gem 'icalendar', '1.1.4'
# event-calendar for managing calendar
gem 'event-calendar', :require => 'event_calendar'
# paperclip for easy adding of images to articles
gem "paperclip", "~> 2.3"
# grackle for downloading twitter feed
gem 'grackle'

# Bundle gems for the local environment. Make sure to
# put test-only gems in this group so their generators
# and rake tasks are available in development mode:
group :development, :test do
#   gem 'webrat'
# 	To use debugger
# 	gem 'ruby-debug'
end
