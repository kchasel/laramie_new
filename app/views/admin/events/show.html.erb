<h1><%= link_to "Home", :controller => 'main', :action => 'index' %> >> <%= link_to "Events", admin_events_path %> >> <%= @event.name %></h2>
<p><h2>Start Date &amp; Time: <%= @event.start_at.strftime("%A, %B %e at %l:%M%p") %>
	<br />End Date &amp; Time: <%= @event.end_at.strftime("%A, %B %e at %l:%M%p") %>
<p><h5>Event Description: <%= @event.description %></h5></p>

<div id="article_options">
	<h2>Event Options</h2>
<ul>
<li><%= button_to "View this event on the main site", { :action => 'show', :controller => '/events', :id => @event.id }, :confirm => "You are about to leave the admin site. To return, add the word admin after the first slash in the url (after the .org). Ex: #{url_for(:controller => '/main', :action => 'index', :only_path => false)}[insert 'admin' here]", :method => :get %></li>
<li><%= button_to "Edit this event content", edit_admin_event_path(@event), :method => :get %></li>
<li><%= button_to "Delete this event", admin_event_path(@event), :method => :delete, :confirm => "Are you certain you want to delete this event?" %></li>
</ul>
</div>